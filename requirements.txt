# Main Dependencies - Keep in sync with pyproject.toml [tool.poetry.dependencies]
# This file should only contain DIRECT dependencies, not transitive ones
falkordb>=1.0.9
typing-extensions>=4.12.1
bs4>=0.0.2
pypdf>=4.2.0
backoff>=2.2.1
requests>=2.32.3
ratelimit>=2.2.1
python-dotenv>=1.0.1
fix-busted-json>=0.0.18
rich>=13.9.4
ipykernel>=6.29.5
litellm>=1.73.1
ollama>=0.5.1

# Optional AI Model Dependencies (install with extras)
# pip install graphrag-sdk[openai] for OpenAI
# pip install graphrag-sdk[vertexai] for Vertex AI  
# pip install graphrag-sdk[google-generativeai] for Google AI
# pip install graphrag-sdk[all] for all AI models